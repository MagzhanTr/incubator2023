# Вывод таблицы будет очень красивым в терминале, не стал менять)

# Установка необходимых библиотек
pip install matplotlip

# Если выведет в консоль Bad Request(400 вроде)
В переменной url, замените путь к источнику данных на корректный. Скорее всего истек срок доступа к файлу.